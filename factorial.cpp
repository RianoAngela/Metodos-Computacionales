#include<iostream>
using namespace std;

int factorial(int);
int main()
{
    factorial(7);
    return 0;
}

int factorial(int n)
{
    for (int i=1; i<=n;i++)
    {
        int factor=factor*i;
    }
}